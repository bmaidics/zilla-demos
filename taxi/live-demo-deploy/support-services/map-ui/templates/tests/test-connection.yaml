apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "map-ui.fullname" . }}-test-connection"
  labels:
    {{- include "map-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "map-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
